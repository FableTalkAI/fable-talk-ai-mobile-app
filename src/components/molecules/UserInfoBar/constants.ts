import { UserInfoBarField } from './types.ts';

const TITLES: Record<UserInfoBarField, string> = {
  name: 'common.name',
  email: 'common.email',
  dateOfBirth: 'common.dateOfBirth',
};

export { TITLES };
